id: metaenvproject
entrypoint: api.app:app

endpoints:
  reset: /reset
  step: /step
  state: /state

models:
  action: MetaenvprojectAction
  observation: MetaenvprojectObservation

metadata:
  name: Manufacturing Optimization Environment
  description: AI agent optimizes temperature and speed using real dataset